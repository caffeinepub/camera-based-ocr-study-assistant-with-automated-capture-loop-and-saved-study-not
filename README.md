## Camera-based OCR study assistant with automated capture loop and saved study notes (no exam/quiz cheating)

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
